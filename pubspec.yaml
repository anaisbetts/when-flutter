name: when_rx
description: A new Flutter package project.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.1.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  rxdart: ^0.27.7
  quiver: ^3.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
